#
# This Makefile is generated by mkNewdev for devSup application src directory
#
ifndef HOST_ARCH
  HOST_ARCH:=solaris
endif

EPICS:=/home/EPICS/dvlepics/R3.13.2.0.J0
TOP:=/home/EPICS/dvlepics/R3.13.2.0.J0
BINDIR=${EPICS}/base/bin/${HOST_ARCH}
PATH:=${EPICS}/appSRcebaf/tools:${BINDIR}:${PATH}

EPICS_BASE = $(EPICS)/base
include $(EPICS)/config/CONFIG_APP
include $(EPICS)/config/RULES_ARCHS
